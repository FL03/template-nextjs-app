/**
 * PostCSS configuration file for Tailwind CSS and Typography plugin.
 * This file is used to configure PostCSS plugins for processing CSS.
 * @url https://tailwindcss.com/docs/installation#post-css
 */
const config = {
  plugins: ['@tailwindcss/postcss'],
};

export default config;
