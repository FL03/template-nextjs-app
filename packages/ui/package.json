{
  "main": "build/index.js",
  "name": "@template-nextjs/ui",
  "description": "Types for pzzld-js packages.",
  "private": false,
  "type": "module",
  "version": "0.0.2",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --watch",
    "lint": "echo \"No tests specified\"",
    "test": "echo \"No tests specified\"",
    "publish": "bun run build && bun publish --access public"
  },
  "devDependencies": {
    "@types/node": "^25",
    "typescript": "^5.9.3"
  }
}